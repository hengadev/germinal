---
# Monitoring Handlers

- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: restart monitoring
  ansible.builtin.systemd:
    name: "{{ app_name }}-monitoring"
    state: restarted
    enabled: true
